import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  output: "export",
  basePath: "/tanvi-dev-portfolio",
  assetPrefix: "/tanvi-dev-portfolio/",
};

export default nextConfig;
